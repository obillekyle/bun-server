{
  "name": "bun-server",
  "version": "1.0.0",
  "description": "A ridiculously fast, zero-config full-stack starter built on Bun.",
  "author": "obillekyle",
  "license": "CC0-1.0 & MIT",
  "type": "module",
  "engines": {
    "bun": ">=1.0.0"
  },
  "scripts": {
    "dev": "bun run .server --dev",
    "serve": "bun run .server",
    "db:sync": "bun run .database/sync.ts",
    "test": "bun test"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "latest",
    "bun-types": "^1.3.13"
  },
  "peerDependencies": {
    "typescript": "^6.0.3"
  }
}
