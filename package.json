{
  "name": "bakery",
  "version": "3.0.0",
  "description": "A ridiculously fast, zero-config full-stack starter built on Bun.",
  "author": "obillekyle",
  "license": "CC0-1.0 & MIT",
  "type": "module",
  "main": ".server/index.ts",
  "engines": {
    "bun": ">=1.0.0"
  },
  "scripts": {
    "dev": "bun --smol run .server --dev",
    "serve": "bun --smol run .server",
    "db:sync": "bun --smol run .server/database/sync",
    "test": "bun test"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "latest",
    "bun-types": "^1.3.14"
  },
  "peerDependencies": {
    "typescript": "^6.0.3"
  }
}
